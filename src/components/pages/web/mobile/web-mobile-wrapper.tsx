import React from 'react';
import WebCredentialsList from '@/components/pages/web/web-credential-list';

const WebMobileCardWrapper = () => {
  return <WebCredentialsList />;
};

export default WebMobileCardWrapper;
